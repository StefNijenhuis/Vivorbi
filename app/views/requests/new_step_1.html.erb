<% content_for :title, "Nieuwe oproep" %>
    
<%= form_for @request, url: @base_form_path+@form_target do |f| %>
  <div class="row">
    <div class="small-12 columns">
      <!-- STEP 1 - TITLE OF REQUEST -->
      <h1 class="form_title" id="title_name">Wat is uw oproep?</h1>
    </div>
  </div>
  <div class="row">
    <div class="small-12 columns">
      <h4>
        <%= f.label :title, "Wat voor oproep wilt u plaatsen?" %>
      </h4>
    </div>
  </div>
  <div class="row">
    <div id="request_form_name" class="small-12 columns">
      <% if @request.errors.any? %>
        <div id="error_explanation">
          <div class="row">
            <div class="small-12 columns">
              <ul>
                <% @request.errors.full_messages.each do |msg| %>
                <%= msg %><br>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      <% end %>
      <%= f.text_field :title, placeholder:"Naam van uw oproep", title:"Minimaal 10 karakters invullen" %>
      <%= f.hidden_field :date %>
    </div>
  </div>
  <!-- PROGRESS BAR AND BUTTONS -->
  <div class="row">
    <div id="progress_bar" class="small-4 push-4 columns">
      <span class="small-12 columns progress_text">
        Stap 1 van 3
      </span>
      <div class="small-4 columns done" id="first"></div>
      <div class="small-4 columns not-done"></div>
      <div class="small-4 columns not-done end" id="last"></div>
    </div>
    <div class="small-4 columns">
      <%= f.submit @submit_text, class:"submit_button" %>
    </div>
  </div>
<% end %>