<h1>Messages show view</h1>
<%=@message.inspect%>

<h2>Comments</h2>
<% @message.comments.each do |comment| %>
  <p><%=comment.inspect%></p>
<% end %>

<h2>Add a comment:</h2>
<%= form_for([@message, @message.comments.build]) do |f| %>
    <%= f.text_area :body %>
    <%= f.submit %>
<% end %>